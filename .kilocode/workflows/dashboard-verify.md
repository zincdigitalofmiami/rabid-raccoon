# Dashboard Verify

1. Read and follow AGENTS.md at the repository root.
2. Identify which dashboard files are affected in src/app/, src/components/, and src/lib/.
3. Review data dependencies and API routes touched by the change.
4. Use Puppeteer to verify rendered behavior when UI changes are involved.
5. Check browser console output and visual regressions before finishing.
6. Summarize any remaining manual validation items.
